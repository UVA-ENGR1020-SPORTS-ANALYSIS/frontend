🎯 **What:** Missing tests for the `fetchPlayerStats` function. Specifically, tests covering a network connection failure (when `fetch` rejects) and handling of invalid JSON responses despite a successful response status.

📊 **Coverage:**
- Added a test ensuring `fetchPlayerStats` throws an error appropriately when `fetch` rejects due to network failure.
- Added a test ensuring `fetchPlayerStats` throws an error when a 200 OK response cannot be parsed as JSON.
- Replicated these two robust error test cases for `fetchTeamPlayers` to ensure consistency.

✨ **Result:** Improved test coverage and reliability for `src/api/players.ts` by safeguarding against unhandled edge cases resulting from network connectivity or invalid data structures.
